; Local development overrides, loaded after php.ini (conf.d/zz-app.ini < conf.d/zz-dev.ini).

; The repository is bind-mounted: pick up edits on the next request.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0

display_errors = On
display_startup_errors = On
